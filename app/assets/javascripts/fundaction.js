
$(document).ready(function(){
  $('form.edit_user select#user_languages').chosen({ width: '100%' });
  $('form.edit_user select#user_country').chosen({ width: '100%' });
  $('form.edit_user select#user_areas_of_interest').chosen({ width: '100%' });
});
